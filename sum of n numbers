// Sum of n numbers

#include <stdio.h>

int main(){
	
	int n;
	printf("Enter the number:");
	scanf("%d", &n);
	
	int sum = 0;
	int i;
	for(i = 0; i <= n; i++){
		sum = sum+i;
	}
	printf("Sum of %d numbers is: %d", n, sum);
	return 0;
}
